apiVersion: v1
kind: Service
metadata:
  name: hello-gitops
  namespace: hello-gitops
spec:
  selector:
    app: hello-gitops
    version: blue  # Start with Blue
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
