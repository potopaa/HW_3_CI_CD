name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.9

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: pytest

  build-and-push-docker:
    runs-on: ubuntu-latest
    needs: build-and-test
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Log in to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image (blue)
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/hello-gitops:blue

    - name: Build and push Docker image (green)
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/hello-gitops:green

    - name: Cleanup local Docker images
      run: docker system prune -f

  deploy-to-eks:
    runs-on: ubuntu-latest
    needs: build-and-push-docker
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v3
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    - name: Update kubeconfig
      run: aws eks update-kubeconfig --region us-east-1 --name hello-gitops-new

    - name: Create Namespace
      run: |
        kubectl create namespace hello-gitops || echo "Namespace already exists"

    - name: Deploy Blue
      run: kubectl apply -f deployment—blue.yaml

    - name: Test Blue Deployment
      run: |
        sleep 10
        kubectl rollout status deployment/hello-gitops-blue -n hello-gitops

    - name: Deploy Green
      run: kubectl apply -f deployment—green.yaml

    - name: Test Green Deployment
      run: |
        sleep 10
        kubectl rollout status deployment/hello-gitops-green -n hello-gitops

    - name: Switch Traffic to Green
      run: kubectl patch service hello-gitops -n hello-gitops -p '{"spec":{"selector":{"app":"hello-gitops","version":"green"}}}'

    - name: Validate Green Deployment
      run: |
        service=$(kubectl get svc hello-gitops -n hello-gitops -o jsonpath='{.status.loadBalancer.ingress[0].hostname}')
        for i in {1..10}; do
          curl -s "http://$service" || exit 1
          sleep 1
        done

    - name: Rollback to Blue
      if: failure()
      run: kubectl patch service hello-gitops -n hello-gitops -p '{"spec":{"selector":{"app":"hello-gitops","version":"blue"}}}'
